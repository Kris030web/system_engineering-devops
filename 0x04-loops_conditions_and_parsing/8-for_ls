#!/usr/bin/env bash
# This script displays the part after the first dash in each file name in the current directory

for file in *; do
  # Skip if the item doesn't contain a dash
  if [[ "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done
